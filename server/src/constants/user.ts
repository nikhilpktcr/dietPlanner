export enum EGender {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum ERoles {
  Admin = "admin",
  User = "user",
}

export enum EStatus {
  Active = "active",
  Inactive = "Inactive",
  Deleted = "Deleted",
}
